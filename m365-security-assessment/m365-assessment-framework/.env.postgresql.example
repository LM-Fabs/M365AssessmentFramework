# PostgreSQL Configuration for M365 Assessment Framework

# Database Connection Settings
POSTGRES_HOST=your-postgres-server.postgres.database.azure.com
POSTGRES_PORT=5432
POSTGRES_DATABASE=m365_assessment
POSTGRES_USER=assessment_app

# For local development (uncomment for local testing)
# POSTGRES_PASSWORD=your_local_password

# Azure PostgreSQL Flexible Server with Managed Identity
# No password needed in production - managed identity handles authentication

# Connection Pool Settings (optional - defaults are provided)
# POSTGRES_MAX_CONNECTIONS=20
# POSTGRES_MIN_CONNECTIONS=5
# POSTGRES_IDLE_TIMEOUT=30000
# POSTGRES_CONNECTION_TIMEOUT=10000

# SSL Settings
# For production, SSL is automatically enabled
# For local development, set to false
# POSTGRES_SSL=true

# Application Settings
NODE_ENV=production

# Examples for Azure Configuration:
# POSTGRES_HOST=myserver.postgres.database.azure.com
# POSTGRES_USER=assessment_app
# POSTGRES_DATABASE=m365_assessment
# NODE_ENV=production
